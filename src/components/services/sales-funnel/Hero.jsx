import { ServiceHero } from "../ServiceDetailTemplate";

export default function Hero() {
  return (
    <ServiceHero
      eyebrow="Sales Funnel"
      title="Turn attention into revenue with a funnel built for every step after the click."
      description="We design sales funnels that align messaging, qualification, page flow, automation, and reporting from first touch to booked call."
    />
  );
}
