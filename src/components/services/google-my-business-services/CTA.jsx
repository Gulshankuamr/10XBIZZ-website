import { ServiceCTA } from "../ServiceDetailTemplate";

export default function CTA() {
  return (
    <ServiceCTA
      title="Want more qualified conversations?"
      description="We can map a lead generation plan around your budget, sales cycle, and target market."
    />
  );
}
