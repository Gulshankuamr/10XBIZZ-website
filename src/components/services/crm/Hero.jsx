import { ServiceHero } from "../ServiceDetailTemplate";

export default function Hero() {
  return (
    <ServiceHero
      eyebrow="CRM Automation"
      title="Build a CRM engine that moves leads without manual follow-up chaos."
      description="We design CRM workflows, pipeline stages, lifecycle automations, and reporting layers that help your sales team close faster with less friction."
    />
  );
}
