import { ServiceHero } from "../ServiceDetailTemplate";

export default function Hero() {
  return (
    <ServiceHero
      eyebrow="Lead Generation"
      title="Generate qualified leads consistently, not just traffic spikes."
      description="From offer design to landing page UX and targeting strategy, we build lead systems that attract intent-rich prospects."
    />
  );
}
